export enum EnumListProductType {
  PURCHASE = 6,
  CART = 7,
  WISHLIST = 8,
  POSTED = 9
}

export enum ListProductTypeFormat {
  PURCHASE = 'Total purchases',
  CART = 'Items in cart',
  WISHLIST = 'Items in wishlist',
  POSTED = 'Products posted'
}
